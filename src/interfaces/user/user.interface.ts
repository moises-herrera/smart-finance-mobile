/**
 * Represents a user.
 */
export interface User {
  /** User id. */
  id: string;

  /** User full name. */
  fullName: string;

  /** User email. */
  email: string;

  /** User country. */
  country: string;

  /** User currency. */
  password: string;
}
